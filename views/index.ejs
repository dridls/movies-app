<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="style.css">
</head>

</head>

<body>

    <div class="app-container">
        <h1>Welcome to the movie app,<%= name %>! Your id is <%= id %>
        </h1>
        <form action="addFavorite" method="POST">
            <label for="favorite">What is your favorite movie?</label>
            <input type="text" id="favorite" name="favorite" />
            <button type="submit">add favorite</button>
        </form>

        <h3><a href="/favorites">Your favorite movie</a></h3>
        <div class="collection">
            <% for (let i=0; i < moviesList.length; i++){ %>
                <div class="one-film">
                    <img src="<%= moviesList[i].poster %>" alt="poster-img">
                    <p>
                        <%= moviesList[i].title %>
                    </p>
                </div>
                <% } %>
        </div>
    </div>

</body>

</html>